<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
</head>

<body class="container">
    <header><%- include('../partials/header'); %></header>

    <main>
        <div class="container">
            <h1>Detail: <%= person.title[0].toUpperCase()+person.title.substr(1) %> <%= person.lastName %></h1>
            <!-- Your code goes here. It should look very similar to other pages hint hint. You'd just need to modify the main content to display a single person.  -->
            <div class="row justify-content-center" style="display: block">
                <img src="<%= person.picture %>">
                <p>
                    <%= person.title[0].toUpperCase()+person.title.substr(1) %>
                    <%= person.firstName %>
                    <%= person.lastName %>
                </p>
                <p>
                    Contact: <%= person.email %>
                </p>
                <p>
                    Firebase ID: <%= person.id %>
                </p>
            </div>
        </div>
    </main>
    <footer><%- include('../partials/footer'); %></footer>

</body>

</html>